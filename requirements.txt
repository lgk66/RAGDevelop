# LangChain 核心包
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# 阿里云通义千问支持
dashscope>=1.20.0

# 向量存储相关（如果使用）
chromadb>=0.5.0

# 现有依赖保持不变
streamlit==1.54.0
pandas==2.3.3
numpy==2.4.2
requests==2.32.5
python-dateutil==2.9.0.post0
pytz==2025.2